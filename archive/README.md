# Archive — superseded O4AA lab artifacts

Nothing in this folder is maintained or shipped. It is kept for reference/history only.

## Canonical sources (edit these, not the archive)
- **Lab modules:** `../module-1..6-*.md` (+ `../lab-architecture.md`, `../lab-intro.md`,
  `../reference/architecture-diagram-source.md`) — the single module set.
- **Org provisioning:** the Node.js servicer the lab-engineering platform actually runs —
  `managed-assets-mirror/src/servicers/labs/techcamp-o4aa/`
  (`TechCampO4AALabServicer.js` + `config.js`). This is the single provisioner.

## modules-screenshots/
Screenshot-staging and embedded-image copies of the modules — `[SCREENSHOT: …]` marker
sourcing, base64-embedded variants (`*.embedded.md`), and the aqua / readiness / runbook
capture docs. Superseded by the canonical `module-*.md`.

## jays_modules/
The original first-draft modules (overview / introduction / conclusion). Superseded by the
current `module-1..6` set.
